import React from 'react';
import {Text, View} from 'react-native';

const ProfileScreen = () => {
  return (
    <View>
      <Text style={{marginTop: '50%', textAlign: 'center'}}>ProfileScreen</Text>
    </View>
  );
};

export default ProfileScreen;
